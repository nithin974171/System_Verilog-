//x,y,x+1,y+1...

class child;
  rand int a[10];
  
  constraint x1{
    foreach(a[i]){
      a[i] inside {[10:100]};
      if(i>1) a[i] == a[i-2] + 1;
    }
  }
endclass

module tb;
  child h1 = new();
  initial begin
    h1.randomize();
    $display("h1.a = %0p",h1.a);
  end
endmodule
